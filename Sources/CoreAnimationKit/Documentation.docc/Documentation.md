# ``CoreAnimationKit``

Render, compose, and animate visual elements.

## Overview

``CoreAnimationKit`` provides high frame rates and smooth animations without burdening the CPU or slowing down your app. ``CoreAnimationKit`` does most of the work of drawing each frame of an
animation for you. You're responsible for configuring the animation parameters, such as the start and end points, and ``CoreAnimationKit`` does the rest. It accelerates the rendering by handing over
most of the work to dedicated graphics hardware.

## Topics

### Layer Basics

- ``CoreAnimationLayer``
- ``CoreAnimationLayerDelegate``
